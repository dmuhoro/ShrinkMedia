node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env

# Android / Gradle
.gradle/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Test / build artifacts
test-results/
htmlcov/
*.db-shm
*.db-wal
*.class
*.apk

# Vercel local link/auth (org/project ids pass through secrets in CI)
.vercel/
# Signing keystores + credentials (FAIL CLOSED: never commit these)
keystore.properties
*.jks
*.keystore
*.keystore.properties
*.pkcs12
*.p12
*.pfx
# Secret / key / password material (regardless of location in the repo)
*passwords.txt
*password*.txt
*.key
*.keychain
*.pem
*secret*
*.secrets
*.env.local
.env.*
!*.md
