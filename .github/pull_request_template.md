## What changed

<!-- Summarise the change and the sprint/decision it belongs to (docs/sprints/, ADR if structural). -->

## Why

<!-- State the problem this fixes — cite the real entry point if it is a guard (Constitution Article I.2). -->

## Testing

- [ ] `npm run lint` green
- [ ] `npm test` green
- [ ] `npm run build` green
- [ ] `./gradlew assembleDebug` green
- [ ] Evidence recorded under `docs/evidence/` (cited command + observed result)
- [ ] `CHANGELOG.md` + sprint record updated
- [ ] Commit is SSH-signed, one piece of work per commit

## Risk / blast radius

<!-- Files, modules, settings keys, manifest permissions touched. Any change to the no-INTERNET invariant must link an ADR. -->

## Doc updates needed

<!-- Name the doc that must change with this PR (docs/current-state.md, docs/architecture.md, etc.). -->